-- Default configuration values.
-- Users can override these via Chalk (config.lua in plugins_data).
return {
    Enabled       = true,
    DebugMode     = false,
    ExtraDoseFix  = true,
    SeleneFix     = true,
    StagedOmegaFix = true,
    TidalRingFix  = true,
}
